ft_range_primes
Expected files: ft_range_primes.c

Allowed functions: malloc, free


Write the following function:
int     *ft_range_primes(int start, int end);

The function must allocate (with malloc()) an array of integers and fill it with all prime numbers found between start and end (inclusive of both bounds), then return a pointer to the first element.

If end > start, numbers should be stored in ascending order.
If start > end, numbers should be stored in descending order.

If no prime numbers exist in the range, return NULL.
If malloc fails, return NULL.

A prime number is a natural number greater than 1 that is only divisible by 1 and itself.

Examples
ft_range_primes(1, 10)    // returns [2, 3, 5, 7]
ft_range_primes(-5, 5)    // returns [2, 3, 5]
ft_range_primes(20, 10)   // returns [19, 17, 13, 11]
ft_range_primes(4, 4)     // returns NULL
ft_range_primes(1, 1)     // returns NULL
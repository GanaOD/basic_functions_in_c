Subject: ft_transcend
Expected files: ft_transcend.c
Allowed functions: malloc, free

Write a function that applies a transformation to each element of an array based on provided criteria.

The function takes an array, its size, and two function pointers:

A predicate function that determines if an element should be transformed
A transformer function that performs the actual transformation


Your function should return a new array containing:

Transformed values for elements that satisfy the predicate
Original values for elements that don't satisfy the predicate



The function must be declared as follows:

int *ft_transcend(int *array, int size,
                int (*predicate)(int),
                int (*transformer)(int));


Function descriptions:

predicate: Returns non-zero if the element should be transformed, 0 otherwise
transformer: Takes an integer and returns its transformed version


Example:
If predicate checks if a number is even, and transformer multiplies by 2:

Input array: [1, 2, 3, 4, 5]
Output array: [1, 4, 3, 8, 5]



Notes:

If malloc fails, return NULL
If array is NULL or size <= 0, return NULL
The original array should not be modified